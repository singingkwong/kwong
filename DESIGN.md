# DESIGN.md

## 布局与展示规则（市场模块）
- 各地市场按 6 大区域分类展示：中国、北美、欧洲、东南亚、印度、其他（南美/日韩/俄罗斯等均归入"其他"）。
- 每条市场新闻独立成一张小卡片（一个事件 = 一张卡），事件的三要点不得拆分到多张卡。

## 交互与状态（三要点解读样式）
- 单条新闻卡内固定呈现三个标签化的解读要点：
  - 关键数据 / 事件 = 蓝色（📊）
  - 影响分析 = 黄色（🔍）
  - 趋势判断 = 绿色（📈）
- 卡片底部附「📌 数据来源：机构名」与「🔗 原文」可点击按钮。

## 图片要求（全报告）
- 默认不注入配图：成品 index.html 不含任何 <img>，run_pipeline.py 默认跳过配图注入。
- 如需配图，运行 `python3 scripts/run_pipeline.py --with-images` 显式开启；配图来自 Agent md 中的远程图片 URL，注入前做可达性校验，不可达自动回退本地 images/*.webp 小图（相对路径）。
- 配图仅在"本周总览""各地市场动态""注塑机会专题"各注入 1 张，每张 <img> 带 width/height（16:9）与 alt，避免布局抖动。

## 原文链接要求（全报告）
- 分板块方案：全篇 <a class='source-link'> 原文链接 ≥ 15 条（市场 6 区每区精选 1-2 条 + 政策/车企/调研/注塑/总览各 ≥1），质量优先于条数。
- 每条链接必须真实可达、与内容匹配，禁止伪造格式。

## 生成方式（分板块独立生成）
- fetch_weekly_text.py 按板块各自独立对话生成（总览/政策/车企/调研/注塑/下周各 1 次，市场板块按 6 区域各 1 次），绕开 Coze API 单轮 5 次工具调用上限（错误 6150），避免单次全板块生成被截断缺板块。
- 市场新闻采用三要点格式：每条含「量化要点」（关键数据）与「产业链影响」（影响分析）字段 + 来源 + 原文链接；趋势判断由渲染层 derive_trend 规则兜底。

## 设计禁忌
- 禁止把一条事件的三个要点（关键事件/影响分析/趋势判断）拆成多张独立小卡片。